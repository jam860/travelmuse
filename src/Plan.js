import { Link } from "react-router-dom" //ignore for now

export function Plan() {
    return (
        <main>
            <h1>This is the plan page. Replace with content.</h1>
        </main>
    )
}