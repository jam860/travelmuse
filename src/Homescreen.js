import { Link } from "react-router-dom" //ignore for now

export function Homescreen() {
    return (
        <main>
            <h1>This is the Homescreen page. Replace with content.</h1>
        </main>
    )
}